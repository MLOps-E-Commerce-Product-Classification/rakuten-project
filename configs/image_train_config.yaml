training:
  learning_rate: 0.001
  num_epochs: 10
  weight_decay: 0.0001
  batch_size: 32

model:
  name: efficientnet_b0
  pretrained: true
  freeze_backbone: false

metrics:
  compute_per_class_f1: true
  main_metric: macro_f1